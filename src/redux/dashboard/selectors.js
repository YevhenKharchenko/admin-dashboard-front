export const selectCustomers = state => state.dashboard?.customers;

export const selectTransactions = state => state.dashboard?.transactions;

export const selectCustomersCount = state => state.dashboard?.customersCount;

export const selectProductsCount = state => state.dashboard?.productsCount;

export const selectSuppliersCount = state => state.dashboard?.suppliersCount;
